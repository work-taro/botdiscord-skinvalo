# 🔐 วิธีล็อกอินบอท Valorant (สำหรับเพื่อน)

บอทใช้คำสั่ง **`/cookies`** ในการล็อกอิน — ปลอดภัยกว่าใส่รหัสผ่านตรงๆ และไม่ติด captcha
ทำตามนี้ทีละขั้น ใช้เวลาแค่ 2 นาที (ทำครั้งเดียว ใช้ได้นานหลายสัปดาห์)

> 💻 แนะนำให้ทำบน **คอมพิวเตอร์** (Chrome หรือ Edge) จะง่ายที่สุด

---

## ขั้นที่ 1 — เปิดลิงก์ล็อกอิน Riot

คัดลอกลิงก์นี้ไปวางใน browser แล้วกด Enter:

```
https://auth.riotgames.com/authorize?redirect_uri=https%3A%2F%2Fplayvalorant.com%2Fopt_in&client_id=play-valorant-web-prod&response_type=token%20id_token&scope=account%20openid&nonce=1
```

- ถ้ายังไม่ได้ล็อกอิน → จะมีหน้าให้ใส่ user/pass ของ Riot (ใส่ captcha ถ้ามี) ให้ล็อกอินให้เสร็จ
- พอล็อกอินเสร็จ หน้าจอจะเด้งไปหน้า **playvalorant** ว่างๆ หรือหน้าขาวๆ → **ถือว่าสำเร็จ ปล่อยไว้แบบนั้น**

---

## ขั้นที่ 2 — เปิดหน้า auth (เพื่อให้เห็น cookie)

เปิดแท็บใหม่ พิมพ์ลิงก์นี้แล้ว Enter:

```
https://auth.riotgames.com
```

> จะขึ้นหน้า **"An error occurred!"** ของ Riot — **ไม่ต้องตกใจ ปกติครับ** เราแค่ต้องการให้ browser มาอยู่บนหน้านี้

---

## ขั้นที่ 3 — เปิด DevTools หา cookie `ssid`

1. กดปุ่ม **F12** บนคีย์บอร์ด (หรือคลิกขวา → Inspect)
2. ดูแถบเมนูด้านบนของกล่องที่เด้งขึ้นมา เลือกแท็บ **`Application`**
   - ถ้าไม่เห็น ให้กดลูกศร `»` เพื่อหาเมนูที่ซ่อนอยู่
3. เมนูด้านซ้าย หาหัวข้อ **`Cookies`** กดลูกศรขยาย แล้วคลิก **`https://auth.riotgames.com`**
4. ในตารางที่ขึ้นมา มีช่อง **Filter** ด้านบน → พิมพ์ `ssid`
5. จะเจอแถวชื่อ **`ssid`** → **ดับเบิลคลิกที่ค่าในคอลัมน์ `Value`** แล้วคัดลอก (Ctrl+C)

> ✅ ค่าที่ถูกต้องจะ **ยาวมาก** และ **ขึ้นต้นด้วย `eyJ`**
> ❌ ถ้าได้ค่าที่ขึ้นต้นด้วย `s%3A` แสดงว่ากดผิดโดเมน (นั่นของ account.riotgames.com) — ให้กลับไปเลือก **auth**.riotgames.com

---

## ขั้นที่ 4 — เอาไปวางในดิสคอร์ด

1. ในเซิร์ฟเวอร์ที่มีบอท พิมพ์ **`/cookies`**
2. ช่อง `cookie` → **วางค่าที่คัดลอกมา** (ตัว `eyJ...`)
3. กด Enter

ถ้าขึ้น **`Successfully logged in **ชื่อ#แท็ก!**`** = เสร็จเรียบร้อย 🎉
ใช้ `/store`, `/nightmarket`, `/point`, `/mission` ได้เลย

---

## ❓ ปัญหาที่เจอบ่อย

| อาการ | วิธีแก้ |
|-------|---------|
| ไม่เห็น `https://auth.riotgames.com` ในเมนู Cookies | กลับไปทำ **ขั้นที่ 2** ใหม่ (ต้องอยู่บนหน้า auth ก่อน F12) |
| เจอ `ssid` แต่ขึ้นต้นด้วย `s%3A` | กดผิดโดเมน — เลือก **auth**.riotgames.com ไม่ใช่ account |
| บอทบอก cookie ไม่ถูกต้อง | คัดลอกไม่ครบ/มีช่องว่างติดมา ลองคัดลอกใหม่ให้ครบทั้งเส้น |
| ใช้ไปสักพักแล้วบอทหลุด | cookie หมดอายุ — ทำขั้นที่ 1–4 ใหม่ได้เลย |

> ⚠️ **อย่ากด Logout บนเว็บ Riot** หลังเอา cookie มา เพราะจะทำให้ cookie หมดอายุทันที (ปิดแท็บเฉยๆ ได้)
> 🔒 cookie นี้ = สิทธิ์เข้าบัญชีคุณ อย่าส่งให้คนอื่น ใส่เฉพาะในบอทที่ไว้ใจเท่านั้น
